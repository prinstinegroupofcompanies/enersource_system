# Local dev: leave unset — Vite proxies /api to localhost:4000
# VITE_API_URL=

# Production (Vercel): Render API URL — /api suffix is optional (auto-appended)
# VITE_API_URL=https://enersource-api.onrender.com
# VITE_API_URL=https://enersource-api.onrender.com/api
